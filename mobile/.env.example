# Origin of the deployed Figures web app (Netlify) — used only to reach
# /.netlify/functions/complete for the AI character-generation feature.
# Optional: src/lib/ai.ts already defaults to https://figurescollege.netlify.app.
# Only set this if the web app ever moves to a different URL.
EXPO_PUBLIC_API_BASE_URL=https://figurescollege.netlify.app
